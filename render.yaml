services:
  - type: web
    name: ultimate-scraper-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python -m patchright install chromium
    startCommand: python -m uvicorn "app.main:create_production_app" --factory --host 0.0.0.0 --port $PORT
